// import looksSame from "looks-same";

export const compareWithLS = async (
  newImageData: Uint8ClampedArray,
  baselineImageData: Uint8ClampedArray
) => {
  // return await new Promise((resolve, reject) => {
  //   looksSame(Buffer.from(newImageData), Buffer.from(baselineImageData), {}, (error, result) => {
  //     if (error) {
  //       reject(error);
  //     }

  //     resolve(result);
  //   });
  // });
};
